service: serverless-api

frameworkVersion: '2'


provider:
  name: aws
  runtime: nodejs12.x
  
  stage: dev
  region: us-east-1

functions:
  hello:
    handler: src/handler.hello
    events:
      - http:
          path: /
          method: get
